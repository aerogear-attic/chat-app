// ModuleContext allows for DI
import { ModuleContext } from "@graphql-modules/core";

// this might be wrong but I am using context to hold all the injectables?
export type MyContext = ModuleContext;
